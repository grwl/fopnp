Once, after installing boot2docker:
	mkfs.ext4 -L boot2docker-data /dev/sda
	reboot

My repo:  git clone git@github.com:grwl/fopnp.git
Upstream: git clone https://github.com/brandon-rhodes/fopnp.git

cd fopnp/playground
git checkout mitm

For development:
	git config --global push.default simple
	git push --set-upstream origin mitm

Buiuld:
	sudo ./build.sh 
	sudo ./launch.sh 

Test (password abc123):
	ssh -p 2201 brandon@localhost curl http://172.17.0.66

Kill all containers:
	sudo docker stop -t=1 `sudo docker ps -q`

